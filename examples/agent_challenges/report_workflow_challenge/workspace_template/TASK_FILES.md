# Task Files

These files are safe challenge inputs:

- `input.md`: structured notes fixture for the report.
- `run-input.json`: workflow run input containing the same notes by value in
  field `text`.

These are not workflow solutions. Write your workflow plan, patches, and other
attempt files in this workspace.
